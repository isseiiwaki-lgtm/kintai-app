## サーバーログイン

SSHキーパスフレーズ ippcseisaku→コンパネ発行のを使う必要があるのでこれいらんかった
ssh -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23


## たまにやるアプデ

sudo apt update && apt upgrade -y


## 時刻合わせ

timedatectl set-timezone Asia/Tokyo
date (確認)


## 一般ユーザーへの変更と鍵のコピー、rootでのパスワード接続以外禁止

一般ユーザーでもSSH接続ができるように、rootの鍵設定をコピーします。
※ubuntuの場合rootではなく既に一般ユーザーのため不要

1. 設定の編集
sudo nano /etc/ssh/sshd_config

- 以下を探して(Ctrl + W)パラメータを記述
>PasswordAuthentication no
>PermitRootLogin no
- Ctrl + O を押す(保存)
- Enter を押す(ファイル名確定)
- Ctrl + X を押す(終了)

「PermitRootLogin yes」を「PermitRootLogin no」に変更し、rootでの直接ログインを禁止します。
「PasswordAuthentication yes」を「no」に変更し、パスワード認証を禁止して鍵認証のみを許可します。
設定を保存し、SSHサービスを再起動します。

2. 設定にミスがないか確認(何も表示されなければOK)
sudo sshd -t

3. 設定適用前に別ターミナルを開き、一般ユーザーでログインテスト

4. SSHサービスを再起動して反映
sudo systemctl restart ssh

## ファイアウォールの設定

- ポートの許可
sudo ufw allow 80
sudo ufw allow 443

- それ以外の拒否(FW有効化)
sudo ufw enable

- 確認
sudo ufw status verbose

## DB操作

1. 管理者（postgresユーザー）に切り替える
sudo -i -u postgres

2. PostgreSQLの対話モード（psql）を起動する
psql

-- 1. ユーザー（ロール）の作成（'myuser' と 'mypassword' は好きなものに変えてください）
CREATE USER ippcdb WITH PASSWORD '6@B4m2Vt';

-- 2. データベースの作成
CREATE DATABASE db_kintai;

-- 3. データベースの所有権をユーザーに付与
GRANT ALL PRIVILEGES ON DATABASE db_kintai TO ippcdb;

3. DBのトンネリング(PowerShellで実施)
ssh -L 5432:127.0.0.1:5432 ubuntu@133.18.123.23 -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" -N
ssh -v -L 5432:127.0.0.1:5432 ubuntu@133.18.123.23 -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" -N
<!-- -vでログ出す -->

4. 権限不足にならないよう権限付与
psql -U postgres -d db_kintai -c "GRANT ALL ON SCHEMA public TO ippcdb;"

AUTH_SECRET 生成：
openssl rand -base64 32
Wm6CBH6aulFIeq3ow8nzkmTKnH9AnjaLnssLD3PvlXE=


## 開発DB同期（スキーマ変更・大規模改修前に必ず実施）

1. 本番データを開発DBにコピー（サーバー上で実行）
bash ~/sync-to-dev.sh

2. .env.local の DATABASE_URL を dev に切り替え
DATABASE_URL=postgresql://ippcdb:6@B4m2Vt@localhost:5432/db_kintai_dev

3. スキーマ変更・動作確認
npx prisma migrate dev

4. 確認OK → 本番に戻す
.env.local → db_kintai に変更
npx prisma migrate deploy

※ sync-to-dev.sh は ~/sync-to-dev.sh に配置済み
※ db_kintai_dev はサーバー上の開発専用DB


## セッション開始時の手順

1. SSH トンネル起動（DB 接続）
ssh -v -L 5432:127.0.0.1:5432 ubuntu@133.18.123.23 -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" -N

2.別ウィンドウで
npm run dev


## ユーザー権限の変更

psql -h 127.0.0.1 -p 5432 -U ippcdb -d db_kintai -c 'UPDATE "User" SET role=''ADMIN''       WHERE email=''h2so34@gmail.com'';'


## 変更が反映されない

再ログインが必要


## 再生成+ローカルサーバー再起動

rm -rf .next && npm run dev


## プロダクションデプロイ手順

【初回】
1. サーバーログイン
ssh -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23

2. リポジトリ取得
cd ~
git clone <リポジトリURL> kintai
cd kintai

3. .env.local 作成
nano .env.local
---
DATABASE_URL=postgresql://ippcdb:<パスワード>@localhost:5432/db_kintai
AUTH_SECRET=<openssl rand -base64 32 で生成>
AUTH_GOOGLE_ID=<Google Cloud ConsoleのクライアントID>
AUTH_GOOGLE_SECRET=<Google Cloud Consoleのシークレット>
AUTH_URL=https://kintai.iwaki-i.online
---

4. ビルド＆起動
npm ci
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 start npm --name kintai -- start
pm2 save
pm2 startup   ← 表示されたコマンドを実行

## 更新デプロイ（2回目以降）

1. サーバーログイン
ssh -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23

2. デプロイスクリプト実行
bash ~/deploy.sh

※ deploy.sh が git pull → npm ci → prisma generate → migrate deploy → build → pm2 restart を一括実行
※ .env.local のロードも含む（prisma migrate に必要）

## デプロイファイルの修正などの方法
方法1: コマンドで編集まで（未ログイン時用）
ssh ubuntu@<サーバーIP> "sed -i 's/pm2 restart kintai/pm2 restart kintai 2>\/dev\/null ||    pm2 start npm --name kintai -- start\npm2 save/' ~/deploy.sh" 

方法2: SSHログインしてから以下
nano ~/deploy.sh

## 初回ベースライン登録（migration追加時に1回だけ必要）

set -a; source .env.local; set +a
npx prisma migrate resolve --applied "<マイグレーション名>"
※ ls prisma/migrations/ でマイグレーション名を確認


## 確認コマンド

pm2 status
pm2 logs kintai --lines 50


## Google OAuth リダイレクトURI

https://kintai.iwaki-i.online/api/auth/callback/google
※ Google Cloud Console → OAuth 2.0 クライアント → 承認済みリダイレクト URI に追加

## 中身確認

cat ~/deploy.sh

## DB一覧表示
PGPASSWORD='6@B4m2Vt' psql -U ippcdb -h localhost -l

## VPS の dev DB をローカルにダンプする手順

>ダンプ…このままだとうまく動かない
<!-- ssh -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23 pg_dump -U ippcdb -h localhost -F c db_kintai_dev" > "D:/dev/260325_勤怠アプリ/.resource/.secret/backup/kintai_dev_$(date +%Y%m%d_%H%M).dump" -->

>レストア…このままだとうまく動かない
<!-- ssh -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23 pg_restore -U ippcdb -h localhost -d db_kintai_dev -c" < "D:/dev/260325_勤怠アプリ/.resource/.secret/backup/kintai_dev_20260526_XXXX.dump" -->

>ダンプ(ログイン状態で)
PGPASSWORD='6@B4m2Vt' /usr/bin/pg_dump -U ippcdb -h localhost -F c db_kintai_dev > ~/kintai_dev_backup.dump 2>&1
cat ~/kintai_dev_backup.dump

>SCP(ローカルのGitBashで実行)
scp -i "C:\Users\kumag\.ssh\kagoya\kagoya.key" ubuntu@133.18.123.23:~/kintai_dev_backup.dump "/d/dev/260325_勤怠アプリ/.resource/.secret/backup/kintai_dev_$(date +%Y%m%d_%H%M).dump"