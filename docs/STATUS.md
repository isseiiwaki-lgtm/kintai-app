# 実装状況 & タスク管理

最終更新: 2026-04-23（ヒアリング・タスク整理）

---

## 実装済み（動作確認済み）

### 一般ユーザー機能
- **ホーム** `/` — 今日の打刻状態・出退勤時刻・勤務時間、今月の実績（出勤日数・合計時間）
- **打刻** `/clock` — 出勤・退勤・外出・戻り・休憩開始/終了。2×2グリッドUI。開発用デバッグパネルあり
- **勤怠記録** `/records` — 月次一覧（日付/曜日/出退勤/残業/状態バッジ）、月提出ボタン（※削除予定）、前後月ナビ
- **申請** `/requests` — 申請一覧・キャンセル機能
- **申請 新規** `/requests/new` — 残業申請/遅刻早退/休暇申請（有給・代休・半日）/修正依頼/その他

### 管理者機能 (ADMIN/APPROVER)
- **勤務状況一覧** `/admin/attendance` — 全ユーザー月次一覧、前後月ナビ
- **勤怠承認** `/admin/approval` — ユーザー別ステータス集計、承認操作（※UI刷新予定）
- **申請承認** `/admin/requests` — 申請一覧・承認/却下操作
- **休日カレンダー** `/admin/holidays` — 祝日一括シード、手動追加・削除
- **ユーザー管理** `/admin/users` — 一覧・新規作成・編集・削除、CSVインポート/エクスポート

### 認証
- Google OAuth (NextAuth v5) + JWT戦略
- 未登録Googleアカウント → `/link` で会社メールと紐付け
- ADMIN/APPROVER/EMPLOYEE の3ロール

---

## P1 タスク（次のスプリント）

### インフラ・環境
- [x] **本番→開発DB同期スクリプト** — `~/sync-to-dev.sh` 作成済み。`db_kintai_dev`（同サーバー）に上書きコピー。2026-04-23 動作確認済み
- [x] **デプロイシェルスクリプト** — `~/deploy.sh` 作成済み。`bash ~/deploy.sh` 一発でデプロイ完結。2026-04-23 動作確認済み

### DBスキーマ変更
- [x] **所定出勤曜日フィールド追加** — `User` モデルに `workSun〜workSat` 追加（デフォルト月〜金）。編集・新規ページにチェックボックスUI。マイグレーション済み

### 小機能（UI調整）
- [x] **新規申請UI改善** — フォーム中央寄せ・テキストエリア5行（戻るボタンは既存）
- [x] **ユーザー管理 従業員コードソート** — 従業員コード列追加・コード順ソート（null は末尾）
- [x] **ホームに従業員コード＋氏名表示** — ホーム上部に本人コード＋氏名追加
- [x] **ユーザー管理 両メール表示** — 会社メール/Gmail両表示・Google連携済みバッジ・未設定はオレンジ警告・編集ページに会社メール入力欄追加
- [x] **ユーザー複製機能** — フォーマット系フィールドをコピーした新規ユーザー作成→編集画面へ遷移
- [x] **申請承認 列追加・幅統一** — `/admin/requests` に「申請日」列追加（種別〜対象日間）。審査中・処理済みで列幅統一。内容・理由列を広め表示
- [x] **SP ログアウト** — 右上アバター円タップ → ポップオーバー → ログアウトボタン
- [x] **申請 修正・削除機能** — 管理者: 処理済みに「修正」（モーダル編集）・「削除」（確認ダイアログ）、審査中は承認/却下のみ。申請者: 審査中のみ「取り下げ」

### 欠勤処理
- [x] **欠勤申請UI** — 申請フォームに「欠勤申請」追加。`absenceType: "absent"` で区別
- [x] **勤怠記録への欠勤反映** — 承認で `isAbsent=true` + 所定時間スナップショット。削除で自動リセット

---

## P2 タスク（メイン機能実装）

### 打刻修正申請フロー刷新
- [ ] **CORRECTION申請タイプ追加** — 新申請種別。申請者が「対象項目（出勤/退勤/外出/帰社/休憩開始/休憩終了）・理由・修正時刻（HH/MMセレクト）」を入力
- [ ] **勤怠記録から修正依頼リンク** — `/records` の各日付行に「修正依頼」リンク追加。対象日・対象項目をプリセット
- [ ] **管理者 修正申請承認** — 承認でDBに自動反映。却下も可
- [ ] **既存 COMMENT タイプは存続** — CORRECTION と別に残す（テキストのみの連絡用途）

### 勤怠承認 管理者詳細UI
- [ ] **`/admin/approval` 刷新** — 現行のユーザーカード一覧 → 個人別月次詳細ビューに変更
  - 勤務状況一覧から社員名クリック → 個人詳細へ遷移
  - 表示列: 日付/曜日・勤務状況・始業・終業・労働時間・休憩・中抜け・残業・欠勤+遅刻+早退・有給(時間)・特別休暇(有給)
  - 行クリック → 編集モーダル（全フィールド編集可、管理者編集で自動APPROVED）
  - 全承認ボタン（月一括承認）
  - 承認申請リンク（申請のある日は申請詳細へ）
- [ ] **AttendanceRecord スキーマ拡張** — 遅刻分・早退分・欠勤分・残業分・有給取得時間などのフィールド追加。原打刻と変更打刻を分離保存

### ホーム インフォメーション欄
- [ ] **ホームにインフォメーションセクション追加** — 打刻漏れ・承認待ち申請・却下申請などをアラート表示
  - 月次提出ボタンはこの機能で代替 → `/records` から削除

### 締め日・集計期間設定
- [ ] **設定ページ復活** `/settings`（管理者用）— サイドバーに再追加
  - 締め日設定（例: 25 → 26〜翌25日が集計期間）
  - 締め日に連動して `/records`・管理者一覧の表示期間が変動
  - 休憩時間ルール設定（例: 8時間以上勤務で60分自動控除）
  - 有給付与ルール設定（将来）

---

## P3 タスク（後続）

- [ ] **申請承認 処理済み件数制限** — 現状は全件取得で増加し続ける。審査中は常に全表示。処理済みは対象月単位で表示し、左右ボタンで前月/翌月へ切替（デフォルト今月）
- [ ] **メール通知** — 優先度: 承認依頼通知 > 打刻漏れ（管理者向け） > 却下通知 > 承認完了通知。`companyEmail` 優先・なければ `email` にフォールバック
- [ ] **マニュアル画面** — MDファイル管理方式。一般ユーザー用・管理者用を分ける。叩き台作成はトークン余裕のある時に
- [ ] **月次提出ボタン削除** — インフォメーション欄（P2）実装後に `/records` から削除

---

## 懸念事項

- **打刻の時刻丸め** — 現在は生時刻を保存。会社の丸めルール（例: 15分単位）が必要か要確認
- **勤務時間計算** — workingMinutes の更新タイミングと外出/休憩控除のロジックを要確認
- [ ] **LeaveBalance テーブル** — migration 済みか未確認。actionDeleteUser で `.catch(()=>{})` で握り潰している
- [ ] **外出・休憩の複数回問題（要仕様確認）** — 現在 `goOutAt/returnAt`・`breakStart/breakEnd` は各1ペアのみ保持。2回目で上書き消滅。対応方針:
  - A) 現行のまま（1日1回外出を前提とし、複数回は修正依頼で対応）
  - B) スキーマに `GoOutLog[]` テーブルを追加して複数回対応
- **出勤日判定の優先順位** — 休暇申請承認済 > 休日カレンダー > 個人所定出勤曜日設定（休日に休暇申請不可などのバリデーションで矛盾を事前排除）
- **AttendanceRecord 原打刻と変更打刻の分離** — 管理者編集時に元の打刻記録を保持するかどうか設計要検討

---

## ISSUE（テスト中の要望・不具合報告）

テスト期間中に上がった要望・不具合を記録する。対応判断・優先順位が決まり次第「未実装/要対応」または「検討事項」へ移動。

| # | 内容 | 報告者 | 日付 | 判断 |
|---|------|--------|------|------|
| - | （テスト開始後に記録） | | | |

---

## 変更履歴

### 2026-04-23
- **ヒアリング・タスク整理** — ISSUE.md全16項目をヒアリングし優先度付けのうえSTATUS.mdに昇格
- **DB同期・デプロイ整備** — `db_kintai_dev` 作成・`sync-to-dev.sh`・`deploy.sh` 完成。prismaマイグレーション初期ベースライン設定完了
- **UI小改修** — 新規申請フォーム中央寄せ・ホーム従業員コード表示・ユーザー管理両メール表示・編集ページ会社メール欄追加
- 月次提出ボタン: 削除方針確定（P3）
- 打刻修正申請フロー: CORRECTION新タイプ追加、1件1項目申請方式に確定
- 勤怠承認UI: 管理者詳細ビュー（個人別月次一覧＋編集モーダル）に刷新方針確定
- 締め日設定: 設定ページ復活・システム全体設定として管理
- 所定出勤曜日: ユーザーごとに設定、デフォルト月〜金

### 2026-04-22
- **打刻UI改修** — ボタンを2×2グリッド（出勤/退勤・外出/休憩）に変更。カラー・シャドウ刷新。PC幅拡大
- **雇用形態に「雇用者」追加** — DB値 `employer`。打刻制限なし、所定時間0扱い
- **従業員コード形式確認** — 数字のみ（例: `706`）で登録可。ゼロパディング処理なし（運用側で対応）
- **CSVインポート UNIQUE制約エラー修正** — employeeCode 競合を事前クリアしてから upsert
- **gitignore 修正** — `_*/` を `/_archive/` `/_disposal/` に限定し `_components/` を追跡対象に
- **TypeScript 型エラー一括修正** — 本番ビルド時の暗黙的 `any` を全ファイルで解消
- **VPSデプロイ完了** — `https://kintai.iwaki-i.online` 公開。PM2+Nginx+PostgreSQL構成

### 2026-04-22 以前
- Google OAuth /link フロー バグ修正（UNIQUE制約・Hydration・cookie 問題）
- ユーザー削除 UI 追加（編集ページ下部）
- サイドバーから「設定」非表示化
- CSV インポート/エクスポート実装
