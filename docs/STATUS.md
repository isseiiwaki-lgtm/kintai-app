# 実装状況 & タスク管理

最終更新: 2026-08-19

---

## 機能一覧（実装済み）

### 一般ユーザー
- **ホーム** `/` — 打刻状態・月次実績。インフォメーション（退勤漏れ・却下・要確認・審査中）。クイック打刻ボタン
- **打刻** `/clock` — 出勤・退勤・外出・戻り・休憩開始/終了（2×2グリッド）。当日コメント欄（申請にならない当日事情を管理者へ連絡。`note` カラム使用・承認詳細の備考列に表示）
- **勤怠記録** `/records` — 月次一覧（出勤〜早退・状態バッジ）。修正依頼リンク。CORRECTION申請状態に連動したバッジ（申請中/修正済）。丸めと差がある日は生打刻を「実 H:MM」で併記
- **申請** `/requests` — 申請一覧・取り下げ
- **申請 新規** `/requests/new` — 残業・早出・遅刻早退・欠勤・休暇（有給/振休/半日）・打刻修正・その他
- **マニュアル** `/manual`

### 管理者 (ADMIN/APPROVER)
- **勤務状況一覧** `/admin/attendance` — 全ユーザー月次一覧・締め日連動・従業員コード順・管理系部署除外
- **勤怠承認** `/admin/approval` — ユーザー別承認状況（未承認/承認済/締め済）。承認/承認取消/締め。個人詳細: 編集モーダル・一括承認・一括締め・生打刻併記（丸めと差がある日のみ）・**代理打刻フォーム**（出退勤いずれの打刻もなかった日に管理者が後日打刻。対象日候補を自動抽出・休日出勤チェックで遅刻早退を抑止・保存後は承認済）
- **申請承認** `/admin/requests` — 承認/却下/修正/削除。修正前値表示（CORRECTION: 修正前時刻、OVERTIME: 定時）。**多段階承認対応**（経路設定部署のみ。進捗バッジ・担当外は操作不可・ADMIN飛び越し承認）。処理済み一覧は申請者順（人ごと→対象日降順グルーピング）/対象日順の切替可
- **承認経路管理** `/admin/approval-routes` — ADMIN専用。部署×step×承認者の登録/上書き/削除。経路未設定部署は一段階承認
- **休日カレンダー** `/admin/holidays` — 祝日一括シード・手動追加/削除
- **ユーザー管理** `/admin/users` — CRUD・複製・CSVインポート/エクスポート
- **設定** `/admin/settings` — 締め日・休憩控除ルール・打刻丸めスイッチ（定時前打刻→定時扱い・定時14分以内の早出/残業→定時きっかり）
- **Excel出力** `/api/admin/export-xlsx` — 個人別日別勤務報告書（1ファイル・人別シート）。33列・未実装列は空欄。締め期間単位。管理系部署（管理者/管理職）・管理外雇用形態を除外・従業員コード昇順・有給使用合計（日/時間）表示
- **変更履歴** `/admin/changelog`
- **打刻異常検知** `GET /api/admin/anomaly-check?date=YYYY-MM-DD` — 退勤漏れ・戻り漏れ・パート休憩打刻漏れ（6h超）・workingMinutes NULL を JSON で返す。ADMIN セッション or `ANOMALY_CHECK_TOKEN`（Bearer、n8n 日次バッチ用）で認証。省略時は JST 昨日

### 認証
- Google OAuth (NextAuth v5) + JWT。ADMIN/APPROVER/EMPLOYEE の3ロール

---

## 未完了タスク

### 優先①: 仕様確認・小修正
- [ ] **丸め方向限定化の遡及補正（要判断）** — 2026-08-19 の roundNear 方向限定化以前に、遅刻（出勤の定時後1〜14分）・早退（退勤の定時前1〜14分）が丸めで消えた日が残っている。`rawClockIn/rawClockOut` が残る日（2026-07-29 デプロイ以降）は復元可能。承認済・LOCKED の日は lateMinutes/earlyLeaveMinutes が確定済みで上書き是非の判断が要る。抽出SQLは `docs/DATA_SPEC_HISTORY.md` 末尾
- [ ] **COMMENT 申請タイプ存続** — CORRECTION と別に残す（テキストのみの連絡用途）
- [ ] **有給カウント（残日数管理）機能新設** — 2026-07-10 調査で判明: `LeaveBalance` テーブルは migration 済み（`totalDays`/`usedDays`/`fiscalYear`）で存在するが、付与・消化・表示ロジックは一切未実装（コード内で参照ゼロ、`actionDeleteUser` の削除処理コメントのみ）。半休の時間計算（`paidLeaveMinutes`）とは独立した別機能のため分離。要件確認が必要な項目: 年度起算日、入社日に応じた付与日数ルール、繰越上限、半休0.5日消化の管理、残数不足時の挙動、表示画面（申請時の残数表示・管理画面）
- [ ] **外出・休憩の複数回対応（仕様決定）** — `goOutAt/returnAt`・`breakStart/breakEnd` は各1ペアのみ。2回目で上書き消滅。A) 現行のまま（修正依頼で対応）/ B) `GoOutLog[]` テーブル追加

### 優先②: 計算・ロジック
- [ ] **パート残業計算（週40h超・割増賃金）** — 週40h超・深夜×1.25・月60h超×1.5は将来実装
- [ ] **休日労働の割増計上（要仕様確認）** — 代理打刻の休日出勤チェックで遅刻・早退は 0 にしたが、残業は「実働 − 所定」のままのため所定未満の休日出勤は残業 0 になる。休日労働を別建てで割増計上するかは未確認（2026-07-29 代理打刻実装時に判明）
- [ ] **設定: 時刻丸め処理** — 日次は1分毎カウント・切り上げのみ可。月次合計は30分単位（31〜59分は切り上げ）
- [ ] **承認遅刻フラグ（健診等の会社承認遅刻）＋休憩自動控除の回避手段** — 2026-07-10 実データ調査（本番DB・熊谷氏7/2）で原因特定済み。実打刻 12:49〜19:02（実働6h13m・373分）に対し `calcLegalBreak(373)=45分` はロジック上正しい動作（労基法6h超ルール通り。バグではない）。真の課題は**フルタイム社員には休憩の実打刻記録がなく、経過時間ベースで機械的に45/60分を推定控除する**点（パートのみ `breakStart/breakEnd` 実打刻を見る分岐がある）。管理者が朝出勤に打刻編集する運用カバー後は 09:00〜19:02（10h03m）→60分控除・`overtimeMinutes` 自動再計算まで正しく動作することも確認済み（`admin/approval/[userId]/actions.ts:74-92`）。**設計要件**: 「遅刻承認フラグ」だけでは休憩控除は解消されない（控除は経過時間ベースで独立に動くため）。次のいずれかを追加要 — A) フラグとは別に「休憩取得なし（実働のみで計算）」トグルを設ける／B) 承認遅刻フラグが立った日はフルタイムでも実打刻方式（`breakStart/breakEnd`）に切替可能にする。所定不足分の賃金扱い（所定満了扱いか実働のみか）も要仕様確認（ISSUE 2026-07-10昇格）

### 優先③: 大規模機能
- [ ] **メール通知** — 承認依頼 > 打刻漏れ（管理者向け） > 却下通知 > 承認完了通知。`companyEmail` 優先

### 優先④: 将来機能
- [ ] **申請データ削除時「削除済」表示** — 管理側削除後もユーザー側に記録を残す
- [ ] **キャリブレーション機能** — 対象日付範囲のデータ補正。変更前後プレビュー・警告・バックアップ付き
- [ ] **パート曜日ごと定時設定** — 曜日により所定終業が異なるケース。DBカラム増加が許容できる段階で実装
- [ ] **代休機能の新設** — 現「振休」（`leaveType="substitute"`）とは法的に別概念の「代休」を別機能として追加（ISSUE 2026-07昇格）

---

## 懸念事項

- **勤務時間計算** — `workingMinutes` の更新タイミングと外出/休憩控除のロジックを要確認
- **出勤日判定の優先順位** — 休暇申請承認済 > 休日カレンダー > 個人所定出勤曜日設定（矛盾は事前バリデーションで排除）
- **【要仕様確認】パート休憩打刻漏れ時の法定休憩** — パートが休憩打刻を忘れると `workingMinutes` 過大計上。フォールバックとして `calcLegalBreak` 適用するか管理担当者に確認（`clock/actions.ts:93-104`）

---

## 変更履歴

### 2026-08-19
- **本番デプロイ完了** — コミット `a62a49f` を VPS へ反映（ユーザー実施）。schema 変更なしで migration なし。動作確認は運用の中で実施予定（9:09 出勤が遅刻9分で計上されること・定時14分以内の早出/残業は従来どおり丸められること）
- **バグ修正: 近傍丸め（roundNearClockTime）が遅刻・早退を消していた** — 9:00 始業のユーザーの 9:09 出勤打刻が 9:00 に丸められ遅刻が計上されない事象の報告を受け調査。`applyRounding()` の roundNear は**前後対称**（`|diffMin| <= 14`）で実装されており、遅刻側・早退側も定時へ吸収していた（コード・DOMAIN_REFERENCE・設定UIラベル・テストがすべて旧仕様で一致。バグではなく仕様の穴）。**方向限定**へ変更: opts に `kind: "in" | "out"` を必須追加し、出勤は定時前14分以内のみ・退勤は定時後14分以内のみ丸める。遅刻側（出勤の定時後）・早退側（退勤の定時前）は丸めず実時刻を記録。`kind` を必須にしたのは tsc に呼び出し漏れを検出させるため（呼び出し3箇所: `clock/actions.ts` 出勤=in・退勤=out、`admin/requests/actions.ts` 早出申請却下時の再丸め=in）。`calcMetrics` に遅刻の猶予はないため、丸め修正がそのまま遅刻分の計上に反映される。旧仕様を固定していたテスト（`定時後14分 8:44 → 8:30`）を新仕様へ差し替え、遅刻側・早退側を丸めないケースを追加（vitest 53件 PASS・tsc クリーン）。設定画面のラベルを「定時の前後14分以内 → 定時きっかり」→「定時14分以内の早出・残業 → 定時きっかり」に修正し、遅刻・早退は丸めない旨を明記。**既存データの遡及修正は未実施**（`rawClockIn/rawClockOut` が残る日は復元可能。承認済・LOCKED の日は late/earlyLeave が保存値として焼き付いている点が論点。要判断）

### 2026-07-29
- **本番デプロイ完了** — コミット `9454215`（"add: 打刻なし日への管理者による手動打刻機能追加"）で VPS へ反映。本コミットには**未コミットのまま残っていた 2026-07-10 分**（半休の本人所定時間ベース計算・ダークモードのライト固定・残業/早出申請の時刻範囲拡張）も同梱されており、これらも同時に本番反映済み。schema 変更なしのため migration は空振り
- **機能追加: 代理打刻（打刻ゼロの日への管理者後日打刻）** — ISSUE 昇格（最優先）。承認詳細 `/admin/approval/[userId]` に折りたたみ式の代理打刻フォームを新設（`_components/ProxyPunchForm.tsx`）。対象日セレクトは締め期間内・本日以前で出退勤いずれの打刻もない日のみを自動抽出（欠勤・有給・締め済の日は候補外／当日コメントだけ残った日は候補に含む）。サーバーアクション `actionAdminCreateRecord` を新設し、レコード新規作成＋`workingMinutes`・遅刻・早退・残業の算出＋`AttendanceChangeLog` 記録までを対話型トランザクションで実施。保存後の状態は「承認済」（既存の管理者直接編集と同じ）。**生打刻 `rawClockIn/rawClockOut` は書かない**（実打刻があった日にしか残さない証跡のため）。既存の表・一括承認・件数カウントには一切手を触れていない。**休日出勤チェックボックス**を設け、ON の日は遅刻・早退を 0 で保存（`calcMetrics` は休日を判定せず所定時刻起算で機械計算するため、休日出勤で架空の遅刻が載るのを防ぐ。判定ロジックはフォーム内に閉じ `calcMetrics` 本体は無変更）。ガード: 既に出勤/退勤がある日・締め済（LOCKED）の日・退勤≦出勤はエラーメッセージで拒否（`actionAdminUpdateRecord` の無言 return と異なり画面に理由を表示）
- **リファクタ: 実働時間計算を `calcWorkingMinutes` に共通化** — `lib/attendance.ts` に新設（外出控除→part は実休憩打刻・full は法定休憩を控除／出勤か退勤が欠ければ null）。今回の新規アクションから使用。既存の同一ロジック3重複（`admin/approval/[userId]/actions.ts` の直接編集・`admin/requests/actions.ts` の CORRECTION 承認）は挙動リスクを避けて今回は据え置き（将来の統合候補）
- **バグ修正: 打刻修正申請の監査証跡欠落** — `admin/requests/actions.ts` の CORRECTION 承認で、対象日にレコードが存在しない場合のみ `AttendanceChangeLog` が記録されていなかった（既存レコードありの経路では記録済み）。作成後の id を使う対話型トランザクションに変更して記録するよう修正
- **バグ修正: 回帰テストが19件実行不能だった** — `vitest.config.ts` が存在せず `@/` エイリアスが解決できないため、`lib/attendance.ts` を import する `tests/attendance.test.ts`（丸め・集計の32件中19件）がファイル単位で失敗し実行されていなかった。原因は 2026-07-10 の `calcScheduledMinutes` 追加時に入った `@/config/attendance.config` import。`vitest.config.ts` を新設して復旧（25件 → 52件が実行・全通過）
- **回帰テスト追加** — `tests/proxy-punch.test.ts`。合意した具体例（平日480分／休日出勤の遅刻早退0／同打刻でチェックなしなら遅刻90分早退150分／パート休憩打刻の有無／外出控除後の6h境界／退勤未入力／JST深夜のUTC変換）8件
- **反映先の確認済み範囲** — `/records`・`/admin/attendance` は締め期間の日付範囲のみで絞り status を見ないため、代理打刻したレコードは本人・一覧の双方に表示される。Excel（`export-xlsx/route.ts:253,255`）は `rec.overtimeMinutes` / `rec.lateMinutes` / `rec.earlyLeaveMinutes` の**保存値をそのまま読む**ため、休日出勤で 0 にした遅刻・早退は Excel でも 0 で出力される（再計算による上書きなし）
- **【要判断】休日出勤の残業計上** — 休日出勤チェックで遅刻・早退は 0 になるが、残業は `calcMetrics` の「実働 − 所定」のままのため、所定（例480分）未満の休日出勤は残業 0 で記録される（例: 休日 10:00〜15:00 は実働300分・残業0）。休日労働の割増を別建てで計上する必要があるかは未確認。優先②「パート残業計算（週40h超・割増賃金）」と同じ賃金計算の論点
- **【要確認】パート休憩打刻漏れ時に残業45分が出る** — 代理打刻の検証中に再確認。所定側（`calcScheduledMinutes`）は法定休憩を控除するが実働側（part）は実休憩打刻のみ控除するため、休憩打刻がないと差分が残業として計上される。通常打刻でも同じく起きる既存挙動のため今回は変更せず。懸念事項に既出（`clock/actions.ts:93-104`）

### 2026-07-10
- **機能追加: 半休（有給）の分数計算を本人所定時間ベースに変更** — `app/(app)/admin/requests/actions.ts` の `applyRequestEffects`。従来は `scheduledMins = 480` ハードコード（`req.user.workStartTime/workEndTime` は include 済みだが未使用だった）。`lib/attendance.ts` に共通関数 `calcScheduledMinutes(workStartTime, workEndTime, employmentType)` を新設し、既存の重複実装（`records/page.tsx`・`admin/approval/[userId]/page.tsx` に同一ロジックがインライン重複）を置換・共通化。半休（am/pm）は本人所定時間の半分を四捨五入。全休も本人所定時間ベースに統一（本人所定時間が480分と異なる社員は計上時間が変化＝合意済みの仕様拡張）。Excel出力（`export-xlsx/route.ts`）の有給使用時間合計もハードコード集計（`half?240:480`）から `AttendanceRecord.paidLeaveMinutes` の保存値合算に変更し、二重計算による食い違いを解消（日数集計 `paidDaysTotal` は0.5/1のまま変更なし）。具体例で検証済み: 所定8:30〜17:30・半休→240分（従来と一致）／所定8:00〜16:45(8h超,60分控除後465分)・半休→233分（四捨五入）／振休等 `leaveType!=="paid"` は無変更（ISSUE 2026-07昇格・kintai-fixワークフローで実施）
- **調査: 有給カウント（残日数管理）機能は未実装と判明** — `LeaveBalance` テーブルは migration 済みで存在するが、付与・消化・表示のロジックはコード上どこからも参照されておらず全くの未実装。半休の時間計算とは独立した機能のため、今回のスコープからは分離し未完了タスクへ追加（要件確認後に着手）
- **バグ修正: ダークモード時の文字見づらさをライト固定で解消** — `app/globals.css` の `@media (prefers-color-scheme: dark)` ブロックを削除し `:root` に `color-scheme: light` を追加。`app/layout.tsx` の Metadata にも `colorScheme: "light"` を追加。OSダークモード設定でもUIは常時ライト表示に固定。agent-browserでダークモードエミュレーションし修正前後をスクリーンショット比較・確認済み（修正前はタイトル・ボタン文字が薄グレーで視認困難、修正後は黒字ではっきり表示）（ISSUE 2026-07-10昇格）
- **機能追加: 残業・早出申請の時間範囲を全時間帯トグルで拡張** — `app/(app)/requests/new/page.tsx`。残業終了時刻（既定13:00〜23:45）・早出開始時刻（既定5:00〜12:00）のセレクトに「すべての時間を表示」チェックを追加、ON時は0:00〜23:45全体を表示。午前のみ出勤で12:30まで残業等の例外に対応。現在時刻に非依存（朝の事前申請も可）。`endTime`/`startTime` は承認処理（`applyRequestEffects`）で参照されず表示専用のため、値域拡張は既存の勤怠計算ロジックに影響なし（ISSUE 2026-07-10昇格・kintai-fixワークフローで実施）
- **調査: 休憩45分控除の実データ検証** — 本番DB実データ（熊谷氏7/2）で `calcLegalBreak` の45分控除は6h超ルール通りの正常動作と確認（バグではない）。真の課題はフルタイム社員の休憩が実打刻でなく経過時間からの推定控除である点。「承認遅刻フラグ」対応は管理者への仕様確認中のため保留

### 2026-07-07
- **機能追加: 生打刻（丸め前実時刻）の保存・表示** — ISSUE 昇格（優先高）。`rawClockIn/rawClockOut` カラム新設（migration `20260707000000_add_raw_clock_times`）。打刻時に丸め適用前の実時刻を常時保存（手入力・修正申請承認では書き込まれない＝実打刻が存在した日だけ残る）。表示は丸めと差がある日のみ /records（PC表・SPカード）と承認詳細（/admin/approval/[userId]）の出勤/退勤セルに「実 H:MM」を小さく併記（列追加なし・横幅不変）。Excel には出力しない（有事は DB 参照）。既存の `originalClockIn/Out`（修正前値）とは別概念で併存。migration は dev・本番とも適用済み（本番デプロイ 2026-07-07 完了・列存在を DB 直査で確認）

### 2026-07-06
- **機能追加: 申請承認の並び順切替** — 処理済み一覧を申請者順（人ごと→対象日降順・同一申請者は「〃」でグルーピング表示）/対象日順で切替（デフォルト申請者順）。申請者・種別列の折り返し防止も実施
- **機能追加: 当日コメント欄** — 打刻画面に当日コメント（200字・出勤前でも保存可）。既存 `note` カラム使用で migration 不要。承認詳細画面に備考列追加
- **リファクタ: 締め期間の共通関数化完了** — 残り5箇所（records / admin/attendance / admin/approval / admin/approval/[userId] / home）のインライン式を `lib/closing.ts` に置換。挙動不変（回帰テストで担保）
- **VPSデプロイ完了** — 本日分すべて本番反映済み（migration `add_approval_route` 適用確認済み）。障害対応: pm2 管理外の野良 next-server（6/17起動）がポート3000を占有し旧ビルドを配信していたのを除去・`pm2 save`。詳細 `docs/DEPLOY_CHECKLIST.md` §5。多段階承認は経路未登録＝従来動作のまま、実地確認後に経路登録して運用開始予定
- **バグ修正: 打刻丸めTZズレ** — `applyRounding` の基準日算出が `getUTCDate()` 先行で JST 0:00〜8:59 の打刻（=UTC前日）時に1日ズレ、朝の丸め（定時前丸め・前後14分丸め）が全滅していた。+9h→日付部品→−9h 方式に修正（`lib/attendance.ts`）。境界値7ケース検証済
- **締め期間の共通化・統一** — `lib/closing.ts` 新設（`getClosingPeriod`/`getDefaultClosingMonth`/`listClosingPeriodDates`）。暦月だった `/admin/requests`（申請承認）と `/api/admin/export-xlsx` を締め期間単位に統一
- **Excel出力改善** — ①対象者を role 基準（ADMIN/APPROVER除外）→部署基準（管理者/管理職のみ除外）に変更し一覧・承認画面と統一。②有給使用合計（日/時間）をヘッダに追加。③日次有給日数の半日判定 truthy バグ修正（全日 "full" が 0.5 と誤カウントされていた）
- **文言修正: 代休→振休** — `leaveType="substitute"` の表示名を「振休」に統一（Excel列名・申請フォーム・申請一覧・承認画面の4ファイル。DB値は不変）
- **改修体制の整備** — `docs/DOMAIN_MAP.md`（修正時必読・短い）/ `docs/DOMAIN_REFERENCE.md`（詳細参照）/ skill `kintai-fix`（修正指示ワークフロー: 5軸確認→before/after合意→実装→検証）新設。CLAUDE.md に参照ルール追記。`docs/IMPROVEMENT_BACKLOG.md`（改善案15件）新設
- **回帰テスト導入（vitest）** — `tests/` に丸め・要確認判定・集計・深夜・法定休憩・締め期間の境界値32テスト。`npm run test` で実行。TZ回帰（JST深夜打刻の丸め）を恒久ガード
- **機能追加: 打刻異常検知 API** — `GET /api/admin/anomaly-check` 新規作成（退勤漏れ・戻り漏れ・パート休憩漏れ・workingMinutes NULL）。n8n 日次通知の土台（`ANOMALY_CHECK_TOKEN` 環境変数で Bearer 認証可）
- **ドキュメント新規** — `docs/DEPLOY_CHECKLIST.md`（デプロイ前チェックリスト）・`docs/DESIGN_MULTISTAGE_APPROVAL.md`（多段階承認設計書）
- **機能追加: 多段階承認（申請承認）** — `ApprovalRoute` テーブル新設（部署×step×承認者）・`Approval` ログ運用開始（`SKIPPED` 追加・Cascade化）。経路設定部署の申請は step 順承認、最終 step で APPROVED+勤怠反映。担当外は操作不可・ADMIN 飛び越し承認・進捗バッジ。経路管理画面 `/admin/approval-routes` 新設。**migration `20260706000000_add_approval_route` 未適用（dev/本番とも。デプロイ時に `prisma migrate deploy` 要）**

### 2026-05-27
- **機能追加: Excel出力** — `GET /api/admin/export-xlsx` 新規作成。個人別日別勤務報告書（1ファイル・人別シート・33列）。`exceljs` 追加。ADMIN/APPROVER除外・従業員コード数値昇順・休日行色分け・A4横印刷設定
- **バグ修正: 打刻丸め** — `roundNear` の適用範囲を定時後のみ→**前後14分以内**に修正（`lib/attendance.ts`）。早出申請・残業申請がある日は `roundNear` を無効化（`clock/actions.ts`）。設定ページ説明文を実態に合わせて更新

### 2026-05-26
- **データ監査** — 1ヶ月試験運用データを精査。旧システム（勤怠RECO）サンプルとの対応確認
- **バグ修正: パート残業** — `export/route.ts` の `scheduled=0` バグ修正。DB の `overtimeMinutes` を参照するよう変更
- **機能追加: 退勤時残業保存** — `actionClockOut` で `overtimeMinutes`（法定1日8h超）を保存
- **機能追加: 一括再計算 API** — `POST /api/admin/recalculate` 新規作成。過去の NULL `workingMinutes` 補完・全退勤済みレコードの `overtimeMinutes` 再計算
- **ドキュメント新規** — `docs/DATA_SPEC_HISTORY.md`（仕様変更×データ影響履歴）・`docs/SESSION_2026-05-26_data-audit.md`（セッション引継ぎ）

### 2026-05-20
- **バグ修正** — 所定9時間バグ（`calcLegalBreak`適用）・打刻修正承認後の集計未更新・申請フロー齟齬（ホームのアラート抑制）・当日遅刻時の修正依頼リンク未表示
- **`/admin/approval` 修正** — SUBMITTED 依存で機能停止 → OPEN→APPROVED→LOCKED に統一。締め日連動。承認取消ボタン追加。管理系部署除外・詳細リンク追加
- **`/records` ステータスバッジ** — CORRECTION申請: PENDING→「申請中」・APPROVED→「修正済」。申請中は修正依頼ボタン非表示（`getDisplayStatus` に `correctionStatus` 第3引数追加）
- **機能追加** — 勤務状況ページ: 従業員コード順・管理系部署非表示 / 打刻修正申請フォーム統一 / 申請承認画面に修正前値表示 / 早出申請（OVERTIME + `overtimeType="earlyStart"`）

### 2026-04-28
- **バグ修正** — トップ未確認判定・`/requests` 種別表記・`scheduledMinutes` 固定値
- **P2 ステータス刷新** — OPEN=未確認廃止。「打刻済」「要確認」表示に統一（`calcNeedsReview`/`getDisplayStatus`）
- **表示項目整理** — `/records` 13列・`/admin/attendance` 所定列追加・`/admin/approval/[userId]` 12列
- **P3 打刻丸めスイッチ** — 定時前打刻→定時・定時±14分→定時きっかり の2スイッチ（Setting テーブル拡張）
- **ホームにクイック打刻ボタン** / **残業申請 終了時刻を 13:00〜 に変更**（パート対応）

### 2026-04-24
- **打刻修正申請フロー完成** — 修正依頼リンク追加・管理者承認でDB自動反映
- **欠勤処理** / **申請 修正・削除機能** / **申請承認 UI改善**（申請日列・月別切替）
- **ホーム インフォメーション** — 4種アラート追加・月次提出ボタン削除
- **AttendanceRecord スキーマ拡張** — `lateMinutes`・`overtimeMinutes`・`originalClockIn/Out`・`AttendanceChangeLog`
- **設定ページ完成**（締め日・休憩控除ルール）/ **変更履歴ページ** / **マニュアル画面**

### 2026-04-23 以前
- DB同期・デプロイ整備（`sync-to-dev.sh`・`deploy.sh`・prismaベースライン）
- VPSデプロイ完了（`https://kintai.iwaki-i.online`）/ Google OAuth `/link` フロー修正
- 打刻UI 2×2グリッド改修 / TypeScript 型エラー一括修正 / CSVインポート/エクスポート実装
